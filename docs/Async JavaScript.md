# Async JavaScript

## Callbacks

```js
const apiCall = getAPIData('/path-to-api', (error, result) => {
  if (error) {
  }

  if (result) {
  }
});
```

## Timeouts and intervals

## Promises

## `async...await`

## Control of async flow

### Promisify
